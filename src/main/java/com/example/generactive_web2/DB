CREATE TABLE Item
(

    id        bigint,
    name      varchar,
    basePrice bigint,
    imageUrl  varchar,
    gp_id     bigint UNIQUE REFERENCES Group (id)

);

CREATE TABLE Group
(
    id bigint PRIMARY KEY,
    name varchar,
    parentGroup varchar,
    item_id bigint REFERENCES Item(id)

);